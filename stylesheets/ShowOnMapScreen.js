import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
	mapImage: {
		flex: 1,
		width: '100%',
		resizeMode: 'contain'
	},
	safeAreaView: {
		flex: 1,
	
	}
})

export default styles